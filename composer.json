{
    "name": "atoms/symfony",
    "description": "Symfony bundle for Atoms: AtomsClient, the inbound callback stack, PSR-17/18 resolution, a Messenger queue bridge, and console wrappers around the atoms binary.",
    "version": "0.3.1",
    "license": "MIT",
    "homepage": "https://atomsphp.dev",
    "support": {
        "docs": "https://docs.atomsphp.dev",
        "issues": "https://github.com/AtomsPHP/atoms/issues",
        "source": "https://github.com/AtomsPHP/atoms"
    },
    "type": "symfony-bundle",
    "keywords": [
        "atoms",
        "symfony"
    ],
    "require": {
        "php": "^8.3",
        "atoms/client": "^0.3",
        "psr/container": "^1.1 || ^2.0",
        "psr/http-factory": "^1.0",
        "psr/http-message": "^1.1 || ^2.0",
        "psr/log": "^2.0 || ^3.0",
        "symfony/config": "^6.4 || ^7.0",
        "symfony/dependency-injection": "^6.4 || ^7.0",
        "symfony/http-foundation": "^6.4 || ^7.0",
        "symfony/http-kernel": "^6.4 || ^7.0",
        "symfony/routing": "^6.4 || ^7.0"
    },
    "suggest": {
        "symfony/messenger": "Routes AtomJob callbacks through Messenger (MessengerQueueBridge); without it, NullQueueBridge throws on first use",
        "symfony/console": "Registers the atoms:deploy / atoms:rollback / atoms:list console wrappers",
        "guzzlehttp/psr7": "Default PSR-17 factory used when psr17_factory is not configured",
        "guzzlehttp/guzzle": "Default PSR-18 client used when no atoms.http_client / ClientInterface service is configured"
    },
    "autoload": {
        "psr-4": {
            "Atoms\\Symfony\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Atoms\\Symfony\\Tests\\": "tests/"
        }
    },
    "config": {
        "sort-packages": true
    }
}
